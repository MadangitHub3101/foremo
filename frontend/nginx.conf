server {
    listen 80;

    location /api/ {
        proxy_pass http://myfirstalb-162822641.ap-south-2.elb.amazonaws.com;
        proxy_set_header Host $host;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}